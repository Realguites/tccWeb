import './Button.css'

export default props => {
    return (
        <div className="defaultButton">
            <button>
        
            
            {props.label}</button>
        </div>
    )
}